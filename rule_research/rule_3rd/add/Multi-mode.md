1. Multi-mode简短规格适用于Commercial, SMB, Consumer, Tablets产品线.
2. Multi-mode取值自HTML Spec的Multi-mode, 直接取值即可. 取值时, 如果包含含有冒号":"的条目, 则只取每个条目冒号":"前的文本, 每个条目在简短规格占据一行.
3. 若HTML Spec不包含Multi-mode信息, 或HTML Spec的Multi-mode只包含"No"开头的条目, 则不生成Multi-mode简短规格.